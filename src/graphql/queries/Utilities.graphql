mutation UploadFile($file: Upload!) {
    upload(file: $file) {
        id
    }
}

query VoteraConfiguration {
    version {
        ios
        android
    }
    agora {
        ProposalFeeRatio
        userServiceTermUrl
        privacyTermUrl
        voteResultUrl
    }
}
