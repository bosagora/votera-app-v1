export enum ProposalFilterType {
    LATEST = '최신순',
    PARTICIPANT = '참여순',
}

export enum FeedFilterType {
    LATEST = '최신순으로 보기',
    READ = '읽은 알림 보기',
    NO_READ = '읽지않은 알림 보기',
}

export enum OpinionFilterType {
    LATEST = '최신순',
    POPULATION = '인기순',
}

export enum TestFilterType {}
